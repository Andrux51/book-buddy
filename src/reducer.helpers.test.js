import * as helpers from './reducer.helpers';

describe('handle genre selection', () => {
    const params = {
        existingChosenGenres: [],
        genreChosen: {
            name: 'action',
            selected: false
        }
    }

    test('', () => {

    });
});
